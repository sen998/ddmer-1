# ddmer-1 博客系统——从零到一的部署指南

## 前言

欢迎来到 ddmer-1 博客系统！这是一个基于 Next.js 14 + Prisma + PostgreSQL + Cloudflare R2 的现代个人博客系统，支持文章、相册、图书、音乐等多种内容形式，以及 GitHub OAuth 登录和评论系统。

本教程将带你从零开始，一步步完成整个系统的部署，即使你是技术新手也能轻松上手。

## 一、准备工作

### 1.1 所需账号

在开始之前，请准备好以下账号（均为免费或免费套餐）：

| 序号 | 账号 | 用途 | 注册地址 |
|------|------|------|----------|
| 1 | GitHub | 代码托管和 OAuth 登录 | https://github.com |
| 2 | Vercel | 网站部署托管 | https://vercel.com |
| 3 | Neon | PostgreSQL 数据库 | https://console.neon.tech |
| 4 | Cloudflare | R2 对象存储 + 可选 CDN | https://dash.cloudflare.com |
| 5 | 自定义域名 | 可选，用于绑定域名 | https://my.dnshe.com/ 或其他域名商 |

### 1.2 所需工具

- 浏览器（推荐 Chrome 或 Firefox）
- 文本编辑器（记事本、VS Code 等）
- 在线随机字符串生成器：https://www.xyutil.com/random-string/zh/

### 1.3 环境变量清单

以下是部署时需要配置的环境变量，请提前准备好：

| 变量名 | 说明 | 是否必须 | 来源 |
|--------|------|----------|------|
| DATABASE_URL | Neon 数据库连接串 | ✅ 必须 | Neon 控制台 |
| SECRET_KEY | JWT 签名密钥（64位随机字符串） | ✅ 必须 | 自己生成 |
| FRONTEND_ORIGIN | 前端域名（末尾不要 /） | ✅ 必须 | Vercel 给的域名 |
| NEXT_PUBLIC_API_URL | 前端 API 地址（可选） | 可选 | 和 FRONTEND_ORIGIN 相同 |
| GITHUB_CLIENT_ID | GitHub OAuth App ID | ✅ 必须 | GitHub |
| GITHUB_CLIENT_SECRET | GitHub OAuth App Secret | ✅ 必须 | GitHub |
| ADMIN_GITHUB_USERS | 允许登录后台的 GitHub 用户名 | ✅ 必须 | 你的 GitHub 用户名 |
| R2_ACCOUNT_ID | Cloudflare 账户 ID | ✅ 必须 | Cloudflare |
| R2_ACCESS_KEY_ID | R2 API Access Key | ✅ 必须 | Cloudflare |
| R2_SECRET_ACCESS_KEY | R2 API Secret Key | ✅ 必须 | Cloudflare |
| R2_BUCKET_NAME | 存储桶名 | ✅ 必须 | Cloudflare |
| R2_PUBLIC_URL | 文件公共访问域名 | 强烈建议 | Cloudflare |
---

## 二、第一步：Fork 代码仓库

1. 打开 https://github.com/wryygx/ddmer-1
2. 点击右上角的 **Fork** 按钮
3. 选择你的 GitHub 账号，确认 Fork
4. 等待几秒，代码就会复制到你的账号下

---

## 三、第二步：创建数据库（Neon）

Neon 提供免费的 PostgreSQL 数据库，非常适合个人项目。

### 3.1 创建项目

1. 打开 https://console.neon.tech，登录
2. 点击页面上的 **Create a project**（创建项目）
3. 在弹出的页面里填写：
   - **Project name**：`ddmer-db`
   - **Region**（地区）：**Singapore**（新加坡，离中国最近）
   - 其他保持默认，点击 **Create project**

### 3.2 获取连接字符串

1. 创建成功后，页面会显示一个 **Connection string**（连接字符串），格式如下：

   ```
   postgresql://用户名:密码@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
   ```

2. 点击旁边的 **复制按钮**，把这一串保存到记事本
3. 这就是 `DATABASE_URL` 的值

> **提示**：如果 Neon 数据库名不是 `neondb`，请在连接字符串末尾把 `neondb` 改成你的实际数据库名。

---

## 四、第三步：创建文件存储（Cloudflare R2）

R2 是 Cloudflare 提供的对象存储服务，免费额度 10GB，用来存放上传的图片、文件等。

### 4.1 创建存储桶（Bucket）

1. 打开 https://dash.cloudflare.com，登录
2. 左侧菜单找到 **R2 Object Storage**，点进去
3. 点 **Create bucket**（创建存储桶）
4. **Bucket name** 填：`ddmer-files`（可自定义）
5. 点 **Create**

### 4.2 配置公共访问

1. 进入刚创建的 bucket，点顶部 **Settings** → **Public access**
2. 点 **Connect domain**，绑定一个自己的域名，比如 `files.yourdomain.com`
   - 如果没有自己的域名，可以用 Cloudflare 提供的临时域名
3. 复制这个域名，前面加上 `https://`，保存到记事本：
   - 例如：`https://files.yourdomain.com`
   - 这就是 `R2_PUBLIC_URL`

### 4.3 创建 API 密钥

1. 还是在 Cloudflare R2 页面，左侧菜单点 **Manage R2 API Tokens**
2. 点 **Create API token**
3. **Permissions**（权限）选 **Object Read & Write**
4. **Specify bucket**（指定存储桶）选刚才创建的 `ddmer-files`
5. 点 **Create**

创建成功后，页面会显示三个值，全部保存到记事本：

| 值 | 对应环境变量 |
|----|-------------|
| Account ID | `R2_ACCOUNT_ID` |
| Access Key ID | `R2_ACCESS_KEY_ID` |
| Secret Access Key | `R2_SECRET_ACCESS_KEY` |

> **注意**：Secret Access Key 只显示一次，一定要保存好！

---

## 五、第四步：创建 GitHub OAuth 应用

GitHub OAuth 用于博客的评论系统和后台登录。

### 5.1 创建应用

1. 打开 https://github.com/settings/developers
2. 点 **OAuth Apps** 标签
3. 点 **New OAuth App**（新建 OAuth 应用）
4. 填写：
   - **Application name**：`ddmer-1-blog`
   - **Homepage URL**：`https://你的域名.vercel.app`
   - **Authorization callback URL**：`https://你的域名.vercel.app/api/auth/github/callback`
5. 点 **Register application**

### 5.2 获取密钥

1. 页面会显示 **Client ID**，复制下来，这是 `GITHUB_CLIENT_ID`
2. 点 **Generate a new client secret**
3. 复制新生成的 **Client Secret**，这是 `GITHUB_CLIENT_SECRET`

> **注意**：Client Secret 只显示一次，一定要保存好！

---

## 六、第五步：在 Vercel 上部署

### 6.1 创建项目

1. 打开 https://vercel.com，登录
2. 点 **Add New → Project**
3. 在列表中找到你 Fork 的仓库，点 **Import**
4. 进入项目配置页：
   - **Framework Preset**：自动识别为 Next.js，不用改
   - **Build Command**：保持默认（`npm run build`）
   - **Install Command**：保持默认（`npm install`）

### 6.2 配置环境变量

点击 **Environment Variables**（环境变量），逐条添加：

| 变量名 | 值（示例） | 来源 |
|--------|-----------|------|
| `DATABASE_URL` | `postgresql://...` | 第三步 Neon 复制的连接串 |
| `SECRET_KEY` | `a3f8e2b...`（64位随机字符串） | 用在线工具生成 |
| `FRONTEND_ORIGIN` | `https://你的域名.vercel.app` | Vercel 给的域名 |
| `NEXT_PUBLIC_API_URL` | `https://你的域名.vercel.app` | 和 FRONTEND_ORIGIN 相同 |
| `GITHUB_CLIENT_ID` | `Iv23li...` | 第五步 GitHub 复制的 |
| `GITHUB_CLIENT_SECRET` | `xxx...` | 第五步 GitHub 复制的 |
| `ADMIN_GITHUB_USERS` | `你的GitHub用户名` | 允许登录后台的用户名 |
| `R2_ACCOUNT_ID` | `xxx...` | 第四步 Cloudflare 复制的 |
| `R2_ACCESS_KEY_ID` | `xxx...` | 第四步 Cloudflare 复制的 |
| `R2_SECRET_ACCESS_KEY` | `xxx...` | 第四步 Cloudflare 复制的 |
| `R2_BUCKET_NAME` | `ddmer-files` | 第四步创建的 bucket 名 |
| `R2_PUBLIC_URL` | `https://files...` | 第四步配置的公共域名 |

> **注意**：
> - `FRONTEND_ORIGIN` 末尾不要加 `/`
> - `SECRET_KEY` 请使用至少 32 位的随机字符串，可以在 https://www.xyutil.com/random-string/zh/ 生成
> - `ADMIN_GITHUB_USERS` 填写你的 GitHub 用户名，多个用户名用英文逗号分隔

### 6.3 部署

点 **Deploy**（部署），等待 1-2 分钟完成构建。

---

## 七、第六步：初始化数据库（最关键！）

Vercel 部署完成后，数据库还是空的，需要导入表结构。

### 7.1 操作步骤

1. 打开 https://console.neon.tech，登录
2. 点击你的项目名（如 `ddmer-db`）
3. 在左侧菜单找到 **SQL Editor**，点击进入
4. 打开本仓库中的 `init.sql` 文件，**全选复制**其内容
5. 在 SQL Editor 的输入框里，按 `Ctrl + V` 粘贴
6. 点击上方的 **Run** 按钮执行
7. 等待几秒，看到 **Success** 提示就成功了

> **提示**：`init.sql` 会创建博客所需的所有数据表和索引，包括用户表、文章表、评论表、相册表等。执行一次即可，不要重复执行。

### 7.2 初始管理员账户

数据库初始化后，系统会自动创建一个默认管理员账户。你也可以通过环境变量 `ADMIN_PASSWORD` 设置管理员密码，首次登录后可修改。

---

## 八、验证部署

部署完成且数据库初始化后，访问以下地址验证：

| 地址 | 预期结果 |
|------|----------|
| `https://你的域名.vercel.app/` | 博客首页 |
| `https://你的域名.vercel.app/admin/` | 后台登录页面 |
| `https://你的域名.vercel.app/api/health` | 返回 JSON 数据 `{"status":"ok"}` |

---

## 九、常见问题

### Q1：部署后页面报错 500？

检查 Vercel 的 **Logs**（日志），最常见的原因是环境变量配置错误，特别是：
- `DATABASE_URL` 中的数据库名是否正确
- `FRONTEND_ORIGIN` 末尾是否多了 `/`
- `SECRET_KEY` 是否已设置

### Q2：GitHub 登录失败？

1. 检查 `GITHUB_CLIENT_ID` 和 `GITHUB_CLIENT_SECRET` 是否正确
2. 检查 GitHub OAuth App 的 **Authorization callback URL** 是否和你的域名完全一致

### Q3：图片上传失败？

1. 检查 R2 相关的四个环境变量是否都已正确配置
2. 确认 R2 存储桶已开启公共访问
3. 确认 `R2_PUBLIC_URL` 已正确配置

### Q4：数据库连接失败？

1. 确认 `DATABASE_URL` 使用的是 Neon 的 **Pooled connection string**（带 `-pooler` 的地址）
2. 确认连接字符串中的数据库名正确（默认为 `neondb`）

---

## 十、完整数据库初始化 SQL

以下是 `init.sql` 的完整内容，用于在 Neon 控制台的 SQL Editor 中执行：

```sql
-- CreateTable: 用户表
CREATE TABLE "user" (
    "id" SERIAL PRIMARY KEY,
    "username" TEXT NOT NULL,
    "hashed_password" TEXT NOT NULL,
    "nickname" TEXT NOT NULL DEFAULT '',
    "avatar" TEXT NOT NULL DEFAULT '',
    "email" TEXT NOT NULL DEFAULT '',
    "bio" TEXT NOT NULL DEFAULT '',
    "is_admin" BOOLEAN NOT NULL DEFAULT false,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 文章分类表
CREATE TABLE "Category" (
    "id" SERIAL PRIMARY KEY,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "description" TEXT NOT NULL DEFAULT '',
    "sort" INTEGER NOT NULL DEFAULT 0,
    "post_count" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 标签表
CREATE TABLE "Tag" (
    "id" SERIAL PRIMARY KEY,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "post_count" INTEGER NOT NULL DEFAULT 0
);

-- CreateTable: 文章表
CREATE TABLE "Post" (
    "id" SERIAL PRIMARY KEY,
    "title" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "description" TEXT NOT NULL DEFAULT '',
    "content" TEXT NOT NULL DEFAULT '',
    "cover" TEXT NOT NULL DEFAULT '',
    "category_id" INTEGER,
    "status" TEXT NOT NULL DEFAULT 'draft',
    "is_pinned" BOOLEAN NOT NULL DEFAULT false,
    "views" INTEGER NOT NULL DEFAULT 0,
    "likes" INTEGER NOT NULL DEFAULT 0,
    "word_count" INTEGER NOT NULL DEFAULT 0,
    "reading_time" INTEGER NOT NULL DEFAULT 0,
    "published_at" TIMESTAMP(3),
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "Post_category_id_fkey" FOREIGN KEY ("category_id") REFERENCES "Category" ("id") ON DELETE SET NULL ON UPDATE CASCADE
);

-- CreateTable: 文章-标签关联表
CREATE TABLE "PostTag" (
    "post_id" INTEGER NOT NULL,
    "tag_id" INTEGER NOT NULL,
    PRIMARY KEY ("post_id", "tag_id"),
    CONSTRAINT "PostTag_post_id_fkey" FOREIGN KEY ("post_id") REFERENCES "Post" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT "PostTag_tag_id_fkey" FOREIGN KEY ("tag_id") REFERENCES "Tag" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable: GitHub 用户表
CREATE TABLE "GitHubUser" (
    "id" SERIAL PRIMARY KEY,
    "github_id" INTEGER NOT NULL,
    "login" TEXT NOT NULL,
    "avatar" TEXT NOT NULL DEFAULT '',
    "bio" TEXT NOT NULL DEFAULT '',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 文章评论表
CREATE TABLE "Comment" (
    "id" SERIAL PRIMARY KEY,
    "post_id" INTEGER NOT NULL,
    "parent_id" INTEGER,
    "github_user_id" INTEGER,
    "email_user_name" TEXT NOT NULL DEFAULT '',
    "email_user_avatar" TEXT NOT NULL DEFAULT '',
    "content" TEXT NOT NULL,
    "likes" INTEGER NOT NULL DEFAULT 0,
    "ip" TEXT NOT NULL DEFAULT '',
    "status" TEXT NOT NULL DEFAULT 'approved',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "Comment_post_id_fkey" FOREIGN KEY ("post_id") REFERENCES "Post" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT "Comment_parent_id_fkey" FOREIGN KEY ("parent_id") REFERENCES "Comment" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT "Comment_github_user_id_fkey" FOREIGN KEY ("github_user_id") REFERENCES "GitHubUser" ("id") ON DELETE SET NULL ON UPDATE CASCADE
);

-- CreateTable: 留言板表
CREATE TABLE "Message" (
    "id" SERIAL PRIMARY KEY,
    "github_user_id" INTEGER,
    "parent_id" INTEGER,
    "content" TEXT NOT NULL,
    "ip" TEXT NOT NULL DEFAULT '',
    "status" TEXT NOT NULL DEFAULT 'approved',
    "likes" INTEGER NOT NULL DEFAULT 0,
    "email_user_name" TEXT NOT NULL DEFAULT '',
    "email_user_avatar" TEXT NOT NULL DEFAULT '',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "Message_parent_id_fkey" FOREIGN KEY ("parent_id") REFERENCES "Message" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT "Message_github_user_id_fkey" FOREIGN KEY ("github_user_id") REFERENCES "GitHubUser" ("id") ON DELETE SET NULL ON UPDATE CASCADE
);

-- CreateTable: 说说/动态表
CREATE TABLE "Chatter" (
    "id" SERIAL PRIMARY KEY,
    "content" TEXT NOT NULL,
    "images" TEXT NOT NULL DEFAULT '[]',
    "mood" TEXT NOT NULL DEFAULT '',
    "likes" INTEGER NOT NULL DEFAULT 0,
    "comments_count" INTEGER NOT NULL DEFAULT 0,
    "status" TEXT NOT NULL DEFAULT 'draft',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 说说评论表
CREATE TABLE "ChatterComment" (
    "id" SERIAL PRIMARY KEY,
    "chatter_id" INTEGER NOT NULL,
    "parent_id" INTEGER,
    "github_user_id" INTEGER,
    "email_user_name" TEXT NOT NULL DEFAULT '',
    "email_user_avatar" TEXT NOT NULL DEFAULT '',
    "content" TEXT NOT NULL,
    "ip" TEXT NOT NULL DEFAULT '',
    "likes" INTEGER NOT NULL DEFAULT 0,
    "status" TEXT NOT NULL DEFAULT 'approved',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "ChatterComment_chatter_id_fkey" FOREIGN KEY ("chatter_id") REFERENCES "Chatter" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT "ChatterComment_parent_id_fkey" FOREIGN KEY ("parent_id") REFERENCES "ChatterComment" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
    CONSTRAINT "ChatterComment_github_user_id_fkey" FOREIGN KEY ("github_user_id") REFERENCES "GitHubUser" ("id") ON DELETE SET NULL ON UPDATE CASCADE
);

-- CreateTable: 相册表
CREATE TABLE "Album" (
    "id" SERIAL PRIMARY KEY,
    "title" TEXT NOT NULL,
    "description" TEXT NOT NULL DEFAULT '',
    "cover" TEXT NOT NULL DEFAULT '',
    "photo_count" INTEGER NOT NULL DEFAULT 0,
    "sort" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 照片表
CREATE TABLE "Photo" (
    "id" SERIAL PRIMARY KEY,
    "album_id" INTEGER NOT NULL,
    "url" TEXT NOT NULL,
    "caption" TEXT NOT NULL DEFAULT '',
    "orientation" TEXT NOT NULL DEFAULT 'landscape',
    "sort" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "Photo_album_id_fkey" FOREIGN KEY ("album_id") REFERENCES "Album" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable: 项目表
CREATE TABLE "Project" (
    "id" SERIAL PRIMARY KEY,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "description" TEXT NOT NULL DEFAULT '',
    "long_description" TEXT NOT NULL DEFAULT '',
    "cover_image" TEXT NOT NULL DEFAULT '',
    "tech_stack" TEXT NOT NULL DEFAULT '[]',
    "link_github" TEXT NOT NULL DEFAULT '',
    "link_gitee" TEXT NOT NULL DEFAULT '',
    "link_live" TEXT NOT NULL DEFAULT '',
    "link_docs" TEXT NOT NULL DEFAULT '',
    "status" TEXT NOT NULL DEFAULT 'developing',
    "status_label" TEXT NOT NULL DEFAULT '',
    "is_featured" BOOLEAN NOT NULL DEFAULT false,
    "sort" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 友情链接表
CREATE TABLE "FriendLink" (
    "id" SERIAL PRIMARY KEY,
    "name" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "avatar" TEXT NOT NULL DEFAULT '',
    "description" TEXT NOT NULL DEFAULT '',
    "sort" INTEGER NOT NULL DEFAULT 0,
    "is_approved" BOOLEAN NOT NULL DEFAULT false,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 书签分类表
CREATE TABLE "BookmarkCategory" (
    "id" SERIAL PRIMARY KEY,
    "name" TEXT NOT NULL,
    "icon" TEXT NOT NULL DEFAULT '',
    "description" TEXT NOT NULL DEFAULT '',
    "sort" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 书签站点表
CREATE TABLE "BookmarkSite" (
    "id" SERIAL PRIMARY KEY,
    "category_id" INTEGER NOT NULL,
    "name" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "icon" TEXT NOT NULL DEFAULT '',
    "description" TEXT NOT NULL DEFAULT '',
    "platforms" TEXT NOT NULL DEFAULT '[]',
    "sort" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "BookmarkSite_category_id_fkey" FOREIGN KEY ("category_id") REFERENCES "BookmarkCategory" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable: 音乐表
CREATE TABLE "Music" (
    "id" SERIAL PRIMARY KEY,
    "title" TEXT NOT NULL,
    "artist" TEXT NOT NULL DEFAULT '',
    "cover" TEXT NOT NULL DEFAULT '',
    "src" TEXT NOT NULL,
    "lrc" TEXT NOT NULL DEFAULT '',
    "lrcSrc" TEXT NOT NULL DEFAULT '',
    "type" TEXT NOT NULL DEFAULT 'local',
    "sort" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 站点配置表
CREATE TABLE "SiteConfig" (
    "id" SERIAL PRIMARY KEY,
    "key" TEXT NOT NULL,
    "value" TEXT NOT NULL DEFAULT '',
    "description" TEXT NOT NULL DEFAULT '',
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 访客记录表
CREATE TABLE "Visitor" (
    "id" SERIAL PRIMARY KEY,
    "ip" TEXT NOT NULL,
    "path" TEXT NOT NULL DEFAULT '',
    "user_agent" TEXT NOT NULL DEFAULT '',
    "city" TEXT NOT NULL DEFAULT '',
    "region" TEXT NOT NULL DEFAULT '',
    "country" TEXT NOT NULL DEFAULT '',
    "district" TEXT NOT NULL DEFAULT '',
    "org" TEXT NOT NULL DEFAULT '',
    "asn" TEXT NOT NULL DEFAULT '',
    "is_mobile" BOOLEAN NOT NULL DEFAULT false,
    "is_proxy" BOOLEAN NOT NULL DEFAULT false,
    "is_hosting" BOOLEAN NOT NULL DEFAULT false,
    "browser" TEXT NOT NULL DEFAULT '',
    "os" TEXT NOT NULL DEFAULT '',
    "device_type" TEXT NOT NULL DEFAULT '',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 登录日志表
CREATE TABLE "login_log" (
    "id" SERIAL PRIMARY KEY,
    "user_id" INTEGER NOT NULL DEFAULT 0,
    "username" TEXT NOT NULL DEFAULT '',
    "ip" TEXT NOT NULL DEFAULT '',
    "address" TEXT NOT NULL DEFAULT '',
    "system" TEXT NOT NULL DEFAULT '',
    "browser" TEXT NOT NULL DEFAULT '',
    "summary" TEXT NOT NULL DEFAULT '',
    "operating_time" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 图书分类表
CREATE TABLE "book_category" (
    "id" SERIAL PRIMARY KEY,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "description" TEXT NOT NULL DEFAULT '',
    "sort" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable: 图书表
CREATE TABLE "book" (
    "id" SERIAL PRIMARY KEY,
    "title" TEXT NOT NULL,
    "author" TEXT NOT NULL DEFAULT '',
    "cover" TEXT NOT NULL DEFAULT '',
    "description" TEXT NOT NULL DEFAULT '',
    "file_url" TEXT NOT NULL,
    "format" TEXT NOT NULL DEFAULT 'epub',
    "file_size" INTEGER NOT NULL DEFAULT 0,
    "category_id" INTEGER,
    "sort" INTEGER NOT NULL DEFAULT 0,
    "views" INTEGER NOT NULL DEFAULT 0,
    "chapter_count" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "book_category_id_fkey" FOREIGN KEY ("category_id") REFERENCES "book_category" ("id") ON DELETE SET NULL ON UPDATE CASCADE
);

-- CreateTable: 图书章节表
CREATE TABLE "book_chapter" (
    "id" SERIAL PRIMARY KEY,
    "book_id" INTEGER NOT NULL,
    "title" TEXT NOT NULL,
    "href" TEXT NOT NULL,
    "order" INTEGER NOT NULL DEFAULT 0,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "book_chapter_book_id_fkey" FOREIGN KEY ("book_id") REFERENCES "book" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable: 阅读进度表
CREATE TABLE "reading_progress" (
    "id" SERIAL PRIMARY KEY,
    "book_id" INTEGER NOT NULL,
    "chapter_id" INTEGER,
    "chapter_title" TEXT NOT NULL DEFAULT '',
    "position" REAL NOT NULL DEFAULT 0,
    "updated_at" TIMESTAMP(3) NOT NULL,
    CONSTRAINT "reading_progress_book_id_fkey" FOREIGN KEY ("book_id") REFERENCES "book" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable: 图书笔记表
CREATE TABLE "book_note" (
    "id" SERIAL PRIMARY KEY,
    "book_id" INTEGER NOT NULL,
    "chapter_id" INTEGER,
    "text" TEXT NOT NULL,
    "note" TEXT NOT NULL DEFAULT '',
    "color" TEXT NOT NULL DEFAULT '#facc15',
    "cfi" TEXT NOT NULL DEFAULT '',
    "chapter_title" TEXT NOT NULL DEFAULT '',
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "book_note_book_id_fkey" FOREIGN KEY ("book_id") REFERENCES "book" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable: 邮箱验证表
CREATE TABLE "email_verification" (
    "id" SERIAL PRIMARY KEY,
    "email" TEXT NOT NULL,
    "code" TEXT NOT NULL,
    "verified" BOOLEAN NOT NULL DEFAULT false,
    "expires_at" TIMESTAMP(3) NOT NULL,
    "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateIndex: 唯一索引
CREATE UNIQUE INDEX "user_username_key" ON "user"("username");
CREATE UNIQUE INDEX "Category_name_key" ON "Category"("name");
CREATE UNIQUE INDEX "Category_slug_key" ON "Category"("slug");
CREATE UNIQUE INDEX "Tag_name_key" ON "Tag"("name");
CREATE UNIQUE INDEX "Tag_slug_key" ON "Tag"("slug");
CREATE UNIQUE INDEX "Post_slug_key" ON "Post"("slug");
CREATE UNIQUE INDEX "GitHubUser_github_id_key" ON "GitHubUser"("github_id");
CREATE UNIQUE INDEX "Project_slug_key" ON "Project"("slug");
CREATE UNIQUE INDEX "SiteConfig_key_key" ON "SiteConfig"("key");
CREATE UNIQUE INDEX "book_category_name_key" ON "book_category"("name");
CREATE UNIQUE INDEX "book_category_slug_key" ON "book_category"("slug");
CREATE UNIQUE INDEX "reading_progress_book_id_key" ON "reading_progress"("book_id");

-- CreateIndex: 普通索引
CREATE INDEX "Post_slug_idx" ON "Post"("slug");
CREATE INDEX "Post_status_idx" ON "Post"("status");
CREATE INDEX "Post_category_id_idx" ON "Post"("category_id");
CREATE INDEX "Comment_post_id_idx" ON "Comment"("post_id");
CREATE INDEX "Comment_status_idx" ON "Comment"("status");
CREATE INDEX "Comment_github_user_id_idx" ON "Comment"("github_user_id");
CREATE INDEX "Message_status_idx" ON "Message"("status");
CREATE INDEX "Message_parent_id_idx" ON "Message"("parent_id");
CREATE INDEX "Message_github_user_id_idx" ON "Message"("github_user_id");
CREATE INDEX "Chatter_status_idx" ON "Chatter"("status");
CREATE INDEX "ChatterComment_chatter_id_idx" ON "ChatterComment"("chatter_id");
CREATE INDEX "ChatterComment_status_idx" ON "ChatterComment"("status");
CREATE INDEX "ChatterComment_github_user_id_idx" ON "ChatterComment"("github_user_id");
CREATE INDEX "Photo_album_id_idx" ON "Photo"("album_id");
CREATE INDEX "BookmarkSite_category_id_idx" ON "BookmarkSite"("category_id");
CREATE INDEX "Music_type_idx" ON "Music"("type");
CREATE INDEX "Music_sort_idx" ON "Music"("sort");
CREATE INDEX "Visitor_ip_idx" ON "Visitor"("ip");
CREATE INDEX "Visitor_created_at_idx" ON "Visitor"("created_at");
CREATE INDEX "login_log_user_id_idx" ON "login_log"("user_id");
CREATE INDEX "login_log_operating_time_idx" ON "login_log"("operating_time");
CREATE INDEX "book_category_id_idx" ON "book"("category_id");
CREATE INDEX "book_format_idx" ON "book"("format");
CREATE INDEX "book_chapter_book_id_idx" ON "book_chapter"("book_id");
CREATE INDEX "book_chapter_order_idx" ON "book_chapter"("order");
CREATE INDEX "book_note_book_id_idx" ON "book_note"("book_id");
CREATE INDEX "email_verification_email_idx" ON "email_verification"("email");
CREATE INDEX "email_verification_code_idx" ON "email_verification"("code");

-- SeedData: 默认管理员账户（用户名: admin，密码: admin123，首次登录后请务必修改密码）
INSERT INTO "user" ("username", "hashed_password", "nickname", "is_admin", "created_at", "updated_at")
VALUES ('admin', '$2b$10$XtrNULa4zsesynXMuflT1.QQJG/KNegCPp3FZCphAowDVOqr.B5LO', '管理员', true, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP);
```

> **说明**：SQL 末尾会自动创建一个默认管理员账户，登录后请立即修改密码。

---

## 十一、默认登录信息

数据库初始化完成后，系统会自动创建一个默认管理员账户：

| 项目 | 值 |
|------|-----|
| 后台地址 | `https://你的域名.vercel.app/admin/` |
| 用户名 | `admin` |
| 密码 | `admin123` |

> **安全提示**：首次登录后，请立即在后台修改默认密码！

你也可以使用 GitHub OAuth 登录后台（前提是你的 GitHub 用户名已添加到 `ADMIN_GITHUB_USERS` 环境变量中）。

---

## 十二、结语

恭喜你，到这里博客系统就部署完成了！

你可以通过 `https://你的域名.vercel.app/admin/` 进入后台管理界面，开始发布文章、上传照片、添加项目等内容。

如果在部署过程中遇到问题，欢迎在 [GitHub Issues](https://github.com/wryygx/ddmer-1/issues) 提出，我会尽快回复。

祝你写博客愉快！
